# Backend gateway (NestJS)
LOGIFLOW_API_BASE_URL=http://localhost:3000

# Realtime service (socket.io)
LOGIFLOW_SOCKET_URL=http://localhost:3001

# Front apps can use this for Google Maps integrations (set in local .env only, never commit real keys)
LOGIFLOW_GOOGLE_MAPS_API_KEY=replace-me
